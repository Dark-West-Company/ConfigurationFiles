{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dark West RedM Config File Mapping",
  "description": "Maps local config files to their deployment paths on the Dark West server",
  "type": "object",
  "patternProperties": {
    "^configs/": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The destination path on the server (can include URL-encoded characters like %5B for [)"
        },
        "filename": {
          "type": "string", 
          "description": "The target filename on the server (if different from local filename)"
        }
      },
      "required": ["path"],
      "additionalProperties": false
    }
  },
  "examples": [
    {
      "configs/dw_crafting_config.lua": {
        "path": "server/txData/VORPCore_758630.base/resources/%5BDW_CUSTOM%5D/dw_crafting/config.lua",
        "filename": "config.lua"
      }
    }
  ]
}
